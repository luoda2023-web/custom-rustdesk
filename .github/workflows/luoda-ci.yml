name: LUODA CI

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  call-flutter-build:
    name: Build LUODA
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: nightly
