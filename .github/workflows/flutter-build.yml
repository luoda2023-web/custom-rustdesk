name: Build the flutter version

on:
  workflow_call:
    inputs:
      upload-artifact:
        type: boolean
        default: true
      upload-tag:
        type: string
        default: ""

env:
  FLUTTER_VERSION: "3.24.4"
  RUSTUP_TOOLCHAIN: stable
  VCPKG_COMMIT_ID: "3c69c0d07b47e07da5a89d24d4586be7d65fe13"
  ARMV7_VCPKG_COMMIT_ID: "2fd1cf6d3b06421e18d2d5e2f1b8e6c3d89a1b4"
  VERSION: "1.4.6"
  LIBJPEGTURBO_COMMIT_ID: "3.2.0.1"

jobs:
  generate-bridge:
    runs-on: ubuntu-22.04
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - name: Install Flutter
        run: |
          echo "Flutter version test"
          echo "generate-bridge step works"
